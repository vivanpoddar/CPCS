#include <iostream>
#include <string>
#include <cmath>
#include <bits/stdc++.h>

using namespace std;

#define ull unsigned long long
#define ld long double
#define ll long long
#define endl "\n"
#define pi 3.14159265
#define Gojo ios::sync_with_stdio(0), ios_base::sync_with_stdio(0), cin.tie(0), cout.tie(0);

void setIO(string s)
{
    freopen((s + ".in").c_str(), "r", stdin);
    freopen((s + ".out").c_str(), "w", stdout);
}

int main() {
    setIO("ChooseAFilename");
    // use cin or scanf to take strings, delimited by a space
    // cout will automatically write to the file
}